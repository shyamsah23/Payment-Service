spring:
  application:
    name: Payment-Service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/paymentdb
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:421605}
server:
  port: ${SERVER_PORT:8089}


